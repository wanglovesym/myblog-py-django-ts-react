# ============================================================
# 生产环境配置模板
# ============================================================
# 使用说明：
# 1. 复制此文件: cp .env.prod.example .env.prod
# 2. 修改所有标记为 "change-me" 的值
# 3. .env.prod 不会被提交到 Git（已在 .gitignore）
# ============================================================

# ==================== Django 后端配置 ====================

# 调试模式（生产环境必须关闭！）
DEBUG=0

# 密钥（必须修改！使用随机生成的长字符串）
# 生成方法: python3 -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=change-me-to-a-long-random-secret-key-in-production

# 允许访问的主机（填写你的域名和服务器 IP，逗号分隔）
ALLOWED_HOSTS=localhost,your-domain.com,www.your-domain.com,api.your-domain.com

# CORS 跨域配置（生产环境建议关闭全开放）
CORS_ALLOW_ALL_ORIGINS=false

# ==================== 数据库配置（PostgreSQL） ====================

POSTGRES_DB=myblog
POSTGRES_USER=myblog
# 数据库密码（必须修改！使用强密码）
POSTGRES_PASSWORD=change-me-to-strong-password
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ==================== 前端配置 ====================

# API 基础地址（你的后端域名）
VITE_API_BASE_URL=https://api.your-domain.com
