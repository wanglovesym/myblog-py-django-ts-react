# ============================================================
# Django 开发环境配置模板
# ============================================================
# 使用说明：
# 1. 复制此文件为 .env.dev.django
# 2. 根据实际情况修改配置值
# 3. .env.dev.django 不会被提交到 Git（已在 .gitignore）

# ---------- 调试模式 ----------
DEBUG=1

# ---------- 密钥 ----------
# 开发环境可以用简单值，生产环境必须用随机生成的长字符串
DJANGO_SECRET_KEY=dev-secret-change-me-in-production

# ---------- 允许访问的主机 ----------
# 逗号分隔的主机名/域名列表
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# ---------- CORS（跨域资源共享）配置 ----------
# 开发环境允许所有来源访问 API
CORS_ALLOW_ALL_ORIGINS=true

# ---------- 数据库配置（PostgreSQL） ----------
# 如果这些变量存在，settings.py 会切换到 PostgreSQL
# 注释掉则使用 SQLite（默认）
POSTGRES_DB=myblog
POSTGRES_USER=myblog
POSTGRES_PASSWORD=myblog
POSTGRES_HOST=db
POSTGRES_PORT=5432
