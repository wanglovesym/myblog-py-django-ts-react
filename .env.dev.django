# ============================================================
# Django 开发环境配置文件
# ============================================================
# 这个文件包含敏感信息，需要加入 .gitignore（后面会处理）

# ---------- 调试模式 ----------
# DEBUG=1 表示开启 Django 的调试模式
# 调试模式下会显示详细错误信息、SQL 查询日志等
# 生产环境必须设置为 0（关闭）
DEBUG=1

# ---------- 密钥 ----------
# Django 用于加密会话、CSRF 令牌等的密钥
# 开发环境可以用简单值，生产环境必须用随机生成的长字符串
DJANGO_SECRET_KEY=dev-secret-change-me-in-production

# ---------- 允许访问的主机 ----------
# 逗号分隔的主机名/域名列表
# localhost：本地访问
# 127.0.0.1：本地 IP 访问
# backend：Docker Compose 中的服务名（容器间互访）
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# ---------- CORS（跨域资源共享）配置 ----------
# 开发环境允许所有来源访问 API（前端在不同端口）
# 生产环境应该改为 false，并配置具体的允许域名
CORS_ALLOW_ALL_ORIGINS=true

# ---------- 数据库配置（可选，初期不用） ----------
# 如果这些变量存在，settings.py 会切换到 PostgreSQL
# 初期注释掉，继续使用 SQLite
POSTGRES_DB=myblog
POSTGRES_USER=myblog
POSTGRES_PASSWORD=myblog
POSTGRES_HOST=db
POSTGRES_PORT=5432