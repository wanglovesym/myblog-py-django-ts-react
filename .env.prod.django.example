# ============================================================
# Django 生产环境配置模板
# ============================================================
# 使用说明：
# 1. 复制此文件为 .env.prod.django
# 2. 根据实际情况修改配置值
# 3. .env.prod.django 不会被提交到 Git（已在 .gitignore）

# ---------- 调试模式 ----------
# 生产环境必须设置为 0（关闭调试）
DEBUG=0

# ---------- 密钥 ----------
# 生产环境必须使用随机生成的长字符串（至少 50 字符）
# 生成方法：python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=change-me-to-a-long-random-secret-key-in-production

# ---------- 允许访问的主机 ----------
# 填写你的域名或服务器 IP，多个用逗号分隔
# 例如：ALLOWED_HOSTS=example.com,www.example.com,123.45.67.89
ALLOWED_HOSTS=your-domain.com

# ---------- CORS（跨域资源共享）配置 ----------
# 生产环境建议关闭全开放，使用域名白名单
CORS_ALLOW_ALL_ORIGINS=false
# 如需配置白名单，在 settings.py 中设置 CORS_ALLOWED_ORIGINS

# ---------- 数据库配置（PostgreSQL） ----------
POSTGRES_DB=myblog
POSTGRES_USER=myblog
POSTGRES_PASSWORD=change-me-to-strong-password
POSTGRES_HOST=db
POSTGRES_PORT=5432
