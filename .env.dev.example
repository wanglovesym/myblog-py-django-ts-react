# ============================================================
# 开发环境配置模板
# ============================================================
# 使用说明：
# 1. 复制此文件: cp .env.dev.example .env.dev
# 2. 根据实际情况修改配置值
# 3. .env.dev 不会被提交到 Git（已在 .gitignore）
# ============================================================

# ==================== Django 后端配置 ====================

# 调试模式（开发环境开启）
DEBUG=1

# 密钥（开发环境可用简单值）
DJANGO_SECRET_KEY=dev-secret-change-me-in-production

# 允许访问的主机（逗号分隔）
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# CORS 跨域配置（开发环境允许所有来源）
CORS_ALLOW_ALL_ORIGINS=true

# ==================== 数据库配置（PostgreSQL） ====================

POSTGRES_DB=myblog
POSTGRES_USER=myblog
POSTGRES_PASSWORD=myblog
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ==================== 前端配置 ====================

# API 基础地址（开发环境指向本地后端）
VITE_API_BASE_URL=http://localhost:8000
